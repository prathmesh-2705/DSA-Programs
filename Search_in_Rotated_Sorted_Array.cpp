class Solution {
public:
    int search(vector<int>& nums, int target) {
        int first=nums[1],last=nums[nums.size()];
        int mid=(first+last)/2;
        if(target==nums[mid])
        {
            return mid+1;
            break;
        }
        else if(target<mid)
        {
            
        }
        for(int i=first;i<=last;i++)
        {

        }
        
    }
};
